(ns ssm-datasript.ssm-datasript
  "FIXME: my new org.corfield.new/scratch project.")

(defn exec
  "Invoke me with clojure -X ssm-datasript.ssm-datasript/exec"
  [opts]
  (println "exec with" opts))

(defn -main
  "Invoke me with clojure -M -m ssm-datasript.ssm-datasript"
  [& args]
  (println "-main with" args))
